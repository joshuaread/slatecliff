// Paste your free Web3Forms access key (https://web3forms.com) — emails land in your inbox.
// Until this is set, the form still validates and shows thanks, but does not deliver mail.
window.SLATECLIFF = {
  web3formsAccessKey: "", // e.g. "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
  formSubject: "Slatecliff beta signup",
  fromName: "Slatecliff Waitlist"
};
